import type { NextPage } from "next";
import BackgroundComponent from "../components/BackgroundComponent";

const Home: NextPage = () => {
  return (
    <div>
      <BackgroundComponent />
    </div>
  );
};

export default Home;
